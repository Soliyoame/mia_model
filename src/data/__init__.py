"""数据读取、清洗、切分和过滤模块。

中文说明：本文件是 src/data 包的入口说明(__init__.py)。
data 包对应流水线第 01 步「数据预处理」，把各种原始数据集(Enron 邮件、CUAD 合同、
EDGAR 财报、PubMed 论文等)统一加工成干净、规整、含可攻击实体的文本片段(JSONL)，
供后续建索引、抽事实使用。各子模块分工：
    reader   读取原始文件并抽取正文；
    cleaner  清洗文本(去 HTML/邮件头/签名等噪声)；
    chunker  把长文档切成大小合适的片段；
    filter   按"实体密度"等规则筛掉不适合做攻击样本的片段；
    preprocess  把上面几步串成完整流程。
"""
