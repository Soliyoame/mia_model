# Core runtime dependencies for the current robust auditor pipeline.
#
# Install PyTorch separately according to your CUDA / CPU environment.
# Example:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

openai>=1.0.0
transformers>=4.30.0
datasets>=4.0.0
accelerate>=1.1.0
scikit-learn>=1.0.0
numpy<2.0.0
